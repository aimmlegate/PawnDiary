# Build intermediates
obj/
bin/
*.user
*.suo
.vs/
*.pdb

# OS cruft
Thumbs.db
.DS_Store

# Local Claude Code config (machine-specific)
.claude/

# Local agent/editor/tooling config and indexes
.codegraph/
.codex-worktrees/
.mcp.json
.opencode/
.vscode/
opencode.jsonc
.output/
output/
.agents/
tmp
design/
prompt-lab/
scripts/
INTEGRATIONS.md

# Note: 1.6/Assemblies/PawnDiary.dll (and bundled 0Harmony.dll) are committed on purpose
# so the mod is usable straight from a clone.

# Prompt-lab generated run artifacts
prompt-lab/results/

# Clean Workshop payload produced by scripts/publish.ps1
dist/

# Steam Workshop item id (account/local-specific; publish.ps1 copies it into dist/ regardless)
About/PublishedFileId.txt

