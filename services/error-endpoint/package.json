{
  "name": "pawndiary-error-endpoint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + D1 ingest endpoint for Pawn Diary's opt-out error reporter.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute pawndiary_errors --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute pawndiary_errors --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
